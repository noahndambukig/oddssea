# 2026-08-03

## 12:55 — decision — Journal system adopted from canyoubeatchess

**What happened:** Ported the journal system from the chess project: the
`journal/` tree with its append-only rules and entry types, the `/log` skill
in `.claude/skills/log/`, and the auto-log triggers plus notification line in
`CLAUDE.md`. References adapted to this project — the authoritative-numbers
home is `docs/02-economy/currency-model.md`, and simulation results replace
training-run measurements as the main source of findings.

Other chess conventions were surveyed at the same time and deliberately
deferred, not rejected: build-order dependency tags and exit criteria, a
seams section, work packages, a primer, a risks register with stable IDs,
`implemented-by` frontmatter, and a `tools/` directory. The reason for
deferring: the design is still being fleshed out, and several of those
conventions (work packages, `implemented-by`, `tools/`) only pay off once
code exists.

**Why it matters:** Everything before today survives only in git history and
`docs/decisions/` — the texture of how the cosmetic system was designed is
already partly lost. From here, the raw material for the eventual paper or
blog post accumulates as it happens instead of being reconstructed later.

**Refs:** `journal/README.md`, `CLAUDE.md`, base commit `bf4995e`

## 13:36 — decision — Economy revamp: drop dust, make the second currency wager-earned

**What happened:** Noah decided to revamp the economy around wagering as the
core activity, replacing the dust design. New structure: Currency 1 is earned
via tasks and is the only wagerable currency; bets pay back odds × stake in
Currency 1 as standard betting. Currency 2 is earned *exclusively* through
wagering — "a lot for winning bets, a little for losing ones" — and is what
buys crates and cosmetics. Stated intent: "The whole point is to reward
wagering."

This reverses `docs/decisions/0004-dust-is-a-closed-loop.md` in effect —
dust's closed loop existed to reward collection depth without touching the
coin economy; Currency 2's job is instead to reward gambling volume. It also
resolves the mismatch where the currency model's faucets were written for a
flat-payout match game while the project is a gambling simulator.

Direction is decided; the mechanics are not. Open at time of writing: how C2
rewards scale (flat per bet vs stake-proportional vs odds-scaled), what
inherits dust's jobs (duplicate salvage, set stipends), bankroll-ruin
protection, marketplace denomination, and all actual numbers.

**Why it matters:** Invalidates most of `docs/02-economy/` — currency-model
faucets/sinks, `dust.md` entirely, guardrail metrics, and both simulations.
The compliance rewrite (roadmap Q2/Q5) becomes coupled to this: wagerable C1
plus crates makes "is C1 ever purchasable" the load-bearing regulatory
question. Cosmetics specs (`03-cosmetics/`) survive with sinks re-denominated
to C2.

**Refs:** `docs/decisions/0004-dust-is-a-closed-loop.md`,
`docs/02-economy/currency-model.md`, `docs/02-economy/dust.md`,
`docs/00-project/roadmap.md`, commit `3b1b918`

## 13:46 — decision — Set completion pays a one-time C1 bonus, not a stipend

**What happened:** Resolved the open question from the 13:36 entry about what
inherits dust's jobs. Noah initially wanted set completion to pay uncapped
per-day C1 — "the whole point is that they should be rewarded for completing
the sets so they can gamble more." Argued against: a per-day stipend on a
monotonic counter is a perpetuity that compounds through the core loop (sets →
C1/day → handle → C2 → crates → sets). At an illustrative 100 C1/day per set,
5 completed sets already out-earn the committed task faucet, and ~20 sets
would generate ~5× a new committed player's C2 income — breaking faucet-based
tuning, new-content pacing, flat-balance metrics, and quietly opening a
permanent C2→C1 channel.

Settled: **completion pays a one-time C1 bonus** — no per-day component.
One-time payouts can be generous because they pay per completion event, not
forever. Ongoing veteran rewards (free-play stipends scoped to active-season
sets were sketched as the likely shape) are explicitly **deferred, not
rejected** — out of scope for the current revamp.

Also confirmed this session: duplicate salvage pays lossy C2, marketplace
trades in C2 only (a C1 marketplace would let crate luck convert to bankroll),
and the C2 reward formula is odds-scaled and stake-proportional (win ∝ stake ×
odds, loss consolation ∝ stake) to kill favorite-grinding as a dominant
strategy.

**Why it matters:** The two-currency revamp's structure is now fully settled;
what remains is numbers (house edge, C2 reward coefficients, task faucet
sizes, crate prices in C2, comeback-floor size, completion bonus size) and the
rewrite of `docs/02-economy/` plus new simulations (bankroll-ruin replaces
set-completion as the core sim). The one-time bonus is a bounded C2→C1
conversion, so its size must be derived with the other numbers, not chosen
independently.

**Refs:** 13:36 entry above, `docs/02-economy/dust.md`,
`docs/02-economy/guardrails.md`, commit `3b1b918`

## 14:12 — decision — Currencies named Shells and Pearls; revamp baked into docs

**What happened:** Noah picked **Shells** (C1, wagerable/task-earned) and
**Pearls** (C2, wager-earned, buys cosmetics) from a shortlist. The winning
argument: both reinforce the title's odds+sea pun, cowrie shells were real
historical currency so "wager 500 Shells" reads instantly, and pearls are
rare, sea-born and literally worn — with the bonus metaphor that pearls form
slowly through friction, as Pearls accrue through wagering. Runners-up:
Chips/Pearls, Doubloons/Pearls, Obols/Laurels, Krill/Scales. This resolves
what was roadmap question 6 ("What is the currency called?").

The whole revamp then went into `docs/`: decision entry
`0005-wagering-earns-the-cosmetic-currency.md` written (supersedes 0004,
which is marked superseded in place); `dust.md` marked superseded;
`currency-model.md` and `guardrails.md` banner-flagged as pre-revamp,
rewrite pending the bankroll-ruin simulation; glossary rows Coins/Dust
replaced with Shells/Pearls/Handle; `sets.md` set-bonus table stripped of
daily dust claims (6/6 now grants the one-time Shell bonus); roadmap
rewritten — old Q6 replaced by the wager-economy numbers question, and a
new Q9 added: **what do visitor tips pay now that dust is gone** — an
unresolved consequence of the revamp nobody had noticed until the
verification grep surfaced `flex-layer.md`'s tip-dust line.

**Why it matters:** The docs and the decided design are consistent again —
every remaining `coin`/`dust` mention in `docs/` is either historical
(decision entries), banner-flagged (economy docs awaiting numbers), or
tracked as open (tips). UI copy and future specs can use Shells/Pearls from
here. Next blocking work per the updated README order: core loop, then the
bankroll-ruin simulation and the `02-economy/` rewrite.

**Refs:** `docs/decisions/0005-wagering-earns-the-cosmetic-currency.md`,
`docs/00-project/glossary.md`, `docs/00-project/roadmap.md`,
`docs/README.md`, commit `3b1b918`

## 14:52 — decision — Web-first PWA on web-native TypeScript, hosted on AWS

**What happened:** Platform, client stack and hosting settled in one
discussion. v1 is a responsive web app installable as a PWA; no native
builds or store submission until after the compliance rewrite. Client is
TypeScript + a component framework (React vs Svelte deferred — roadmap
question 10) + PixiJS for WebGL surfaces. Hosting is AWS (Noah has
credits): S3+CloudFront, Lambda+API Gateway, Aurora Serverless v2 Postgres,
Cognito; WebSockets deferred until the core loop exists.

The arguments that decided it: simulated gambling rates 17+/18+ on the
stores and review cycles gate every release, while Shells being
unpurchasable means the stores' payment rails buy nothing; the game is
~90% UI screens plus 2D layered avatars, which is HTML/CSS + fragment-shader
territory; Unity's WebGL export is heavy and weak on mobile browsers,
directly contradicting web-first delivery. Rough cost tiers discussed
(~$0–10/mo dev, ~$40–90/mo soft launch, ~$200–500/mo at ~50k MAU) —
deliberately not recorded in specs because they drift.

Specced as `docs/04-technical/platform.md` and `hosting.md` (both agreed),
decision entry 0006. Two rules baked in as non-negotiable from the first
line of code: mobile-first as a completion gate, and a server-authoritative
economy with ledger-style currency mutations and logged crate opens (the
audit trail behind the published-odds commitment in compliance).

Also found and fixed while filing: the docs tree had production files at
`docs/04-production/` while the README map said `05-production/` and
reserved 04 for technical. Renamed the folder (`git mv`), fixed the two
stale path references (doc-conventions, decision 0001).

**Why it matters:** First technical specs in the repo — `04-technical/` is
no longer empty, and CLAUDE.md's current-state table now tracks economy,
platform and data model separately. The compliance rewrite is now
explicitly upstream of any store submission. Data model
(`04-technical/data-model.md`) is the remaining unwritten technical spec.

**Refs:** `docs/decisions/0006-web-first-on-a-web-native-stack.md`,
`docs/04-technical/platform.md`, `docs/04-technical/hosting.md`,
`docs/00-project/roadmap.md`, commit `3b1b918`

## 15:57 — decision — Launch roster: seven games, five of them communal

**What happened:** Went from "has the game lineup been decided?" (it had
not — `01-game/` was the oldest unwritten spec) through a full grouped menu
of ~30 casino games to a chosen roster. Noah's constraint drove the cut: "I
don't want too many because then my users will be spread too thin." Picked:
**sea races, crash, roulette (communal), lottery (daily + weekly countdown
draws), blackjack (communal tables up to 6), plinko, and dice** — dice
explicitly "mainly for iteration," i.e. the simplest pipeline-proof game.

Notable exclusions: slots (art-heavy, fights pillar 4 — argued against and
not picked), poker and PvP duels (rake economics, deferred), pari-mutuel
race pools (deferred — so races are house-banked bookmaker odds), craps,
baccarat, bingo, mines and the other instant-game variants (role overlap).

Along the way, blackjack surfaced a real economy refinement: Pearl income
scales with handle and handle scales with 1/edge, so blackjack's ~0.5%
skilled edge would let good players farm Pearls ~10× faster than a 5%-edge
game. Fix identified (the real-casino "theo" model): **award Pearls
proportional to theoretical loss — stake × game edge — not raw stake**, so
every game pays the same Pearls per Shell of expected loss. Recorded in
0007 as a shipping condition for blackjack; formula refinement lands with
the economy numbers.

**Why it matters:** Resolves former roadmap question 7 as a side effect —
communal tables and race stands mean avatars are visible during play, so
the flex layer shares the stage rather than carrying all status display.
Real-time sync (WebSockets) moves from "later, maybe" to core scope: five
of seven games need it. `01-game/core-loop.md` is now writable — the next
spec session. Decision entry: `docs/decisions/0007`.

**Refs:** `docs/decisions/0007-seven-game-launch-roster.md`,
`docs/00-project/roadmap.md`, `docs/03-cosmetics/flex-layer.md`, commit
`3b1b918`

## 19:15 — decision — Core-loop pass 1: seasons cut from v1, task faucet shaped

**What happened:** First design pass on `01-game/core-loop.md` (spec not yet
written — this is the talk-first phase). Noah settled the five open
questions:

- **Seasons are cut from v1** — "will overcomplicate V1." The biggest call
  of the pass; everything in `02-economy/` assumed 8-week seasons. Weekly
  becomes the longest cadence. Recorded as `decisions/0008`; roadmap
  questions 3 and 4 rewritten (Set Crate targeting without an active set;
  what seasons look like when they return).
- **Task faucet shape**: daily login (streak-scaled) + daily challenges +
  weekly challenges as the repeatable core; one-time milestone tasks
  (onboarding, first-bet-per-game, set completion per 0005); **referral as
  the flagship big reward** — Noah wants signups prioritised.
- **Session anatomy confirmed**: races on a fixed public cadence as
  appointment beats, instant games and tables filling gaps, lottery
  countdown as the daily site-wide moment.
- **Streaks in** (login streak for v1), **onboarding** = economy intro +
  one free crate + guided tour.

**Why it matters:** The last unspecced system now has an agreed shape;
pass 2 (writing the spec) is unblocked. Cutting seasons simplifies the
pending economy rewrite — set-completion pacing keeps its targets but loses
the deadline framing, and guardrails' rotation-based sink-capacity rule
needs a v1 substitute. Referral paying a wagerable currency is flagged as
the task system's main abuse surface — payout must gate on referee
activity milestones, not signup.

**Refs:** `docs/decisions/0008-no-seasons-in-v1.md`,
`docs/00-project/roadmap.md`, 15:57 entry above, commit `3b1b918`

## 19:22 — milestone — Core loop specced: the last unspecced system has a draft

**What happened:** Pass 2 and 3 of the core-loop session.
`docs/01-game/core-loop.md` (session anatomy: races as the public
metronome, instant games and tables between beats, daily/weekly draws as
site-wide moments, onboarding-as-paid-tour) and `docs/01-game/tasks.md`
(the full Shell faucet: streak-scaled login, three rotating dailies,
weeklies, one-time milestones, referral) both written as drafts. Split into
two files deliberately — the loop is stable, the task pool will churn like
content.

One change from the 19:15 shape: Noah softened the referral gate. Instead
of multi-day activity, the referrer is paid when the referred player's
cumulative task earnings cross a Shell threshold, with the threshold set at
or below the onboarding chain's total payout — easy for a real player,
never triggered by a dormant account. The threshold is explicitly named as
the anti-farming lever, to be raised if instrumentation shows abuse.

Verification pass: grepped `season` across the tree — every remaining
mention is banner-flagged (economy docs), tracked by roadmap questions 3–4
(Set Crate targeting, post-v1 seasons), or sits in post-v1 build phases.
Fixed the one silent contradiction found: `crates.md`'s Set Crate section
still said "active seasonal set" (heading and body) — now "a target set"
with a pointer to 0008 and roadmap question 3.

**Why it matters:** Every system in the game now has at least a draft spec.
The docs' own next-step order flips: the economy rewrite (bankroll-ruin
simulation first) is now item 1, because the loop finally defines the
activities its numbers attach to. Specs to review → agree: core-loop.md,
tasks.md.

**Refs:** `docs/01-game/core-loop.md`, `docs/01-game/tasks.md`,
`docs/03-cosmetics/crates.md`, `docs/README.md`, commit `3b1b918`
