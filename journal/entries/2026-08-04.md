# 2026-08-04

## 00:12 — decision — Launch sets confirmed; set-list's structural ambiguity closed

**What happened:** Noah confirmed the launch sets: **Tidepool, Bloomcycle
and Chrome Dip**, each gaining a Legendary-grade keystone piece in the
shirt slot (the family's treatment pushed to the full Legendary motion
budget — rolling caustic water, blooming petal waves, liquid mirror).
**Void Weave is deliberately not a set**, so the cosmic stays a pure
chase rather than a checklist. Designation recorded in
`skin-roster.md`; roadmap question 10 resolved.

This also closes the structural ambiguity `set-list.md` had carried since
before the revamp — "permanent uniform-tier pool vs seasonal mixed-tier
sets" resolves as: the permanent pool is the only v1 construct, launch
sets are pool families plus a keystone, and the mixed-tier seasonal
concepts (Emberforge and the rest) become the backlog for when seasons
return post-v1. Its old recommendation — keep the cosmic out of any set —
was adopted.

**Why it matters:** Content is now 100% decided — rosters, sets,
keystones, bases. Every art-blocking question is closed. The remaining
pre-code design work is game-modes (started immediately after this),
compliance, and the data model.

**Refs:** `docs/03-cosmetics/content/skin-roster.md`,
`docs/03-cosmetics/content/set-list.md`, `docs/03-cosmetics/sets.md`,
`docs/00-project/roadmap.md`

## 00:20 — decision — Game modes drafted; the lottery is deliberately positive-EV

**What happened:** The game-modes session ran talk-then-write in one
sitting. Settled via pickers: **persistent race roster** (~14 named sea
creatures with visible form; odds always derived from hidden drifting
true probabilities + overround, so form is engagement, never +EV),
**win-only race bets** at launch, **no maximum bet in v1** (all-in is a
modeled archetype; the comeback floor is the safety net), and the
headline: Noah's lottery design — **pot = 1.5× ticket sales, a free 50%
house match** (`decisions/0014`).

The match makes the lottery the only positive-EV game in the house —
deliberately, as a communal generosity moment under the countdown.
Unbounded it is a dominant strategy and an infinite money printer, so
three bounds went in with it: per-player ticket caps sized so the max
expected subsidy ≈ one daily task (~75 Shells/day), no Pearls from
tickets (theo is undefined at negative edge), and the match accounted as
a second, capped Shell faucet — guardrails rule 1 amended from "one
faucet, one sink" to "two faucets, one sink."

`01-game/game-modes.md` drafted (all seven games, disclosure-everywhere
rule, atomic settlement); lottery figures added to `currency-model.md`
marked provisional pending a bankroll-sim lottery term (new roadmap
question 11). Housekeeping: roadmap Q6 (economy numbers) finally struck
— it had been resolved yesterday but never struck.

**Why it matters:** Every launch game now has a written spec; the last
big design gap after this is compliance and the data model. The
positive-EV lottery is the design's most unusual economic object — the
first mechanic whose safety depends on a cap rather than an edge, and
the bankroll sim must confirm the destruction ratio holds before its
numbers lock.

**Refs:** `docs/01-game/game-modes.md`,
`docs/decisions/0014-lottery-house-match.md`,
`docs/02-economy/currency-model.md`, `docs/02-economy/guardrails.md`,
`docs/00-project/roadmap.md`

## 00:26 — finding — Lottery subsidy validated; it inflates Pearls, not Shells

**What happened:** Added the lottery term to `bankroll.py` (EV-smoothed:
committed maxes caps for +775 Shells/wk, casual ~+225) and re-ran. The
surprise: the subsidy is *harmless on the Shell side* — fraction-of-
bankroll staking recycles it completely, committed destruction lands at
**1.02** (balances flat, better than the pre-lottery 0.90) — but it
*leaks into the Pearl side*: recycled subsidy means more handle (58k →
78k/wk), more theo, ~34% more Pearls, pushing committed to ~64 crates/wk
against the ~50 anchor.

Fix per the system's own logic: anchor holds, prices move. Basic crates
70/90 (was 60/80), premium 280/360, Set Crate 100; re-ran `crate-game.py`
— all targets hold (first Legendary median 1.2/3.7 wk, pity ~14%, set
chase 3/8 wk median). Results-of-record, guardrail metrics and roadmap
Q11 updated; lottery figures no longer provisional. Remaining watch item
shifted: it is now the *casual* side (typical 0.89, cautious 0.74 —
hoarders) rather than committed drift.

Also noted mid-session: Noah may want **Mythic prize draws in the
lottery later** — a one-of-one in the weekly pot as a headline event.
Recorded as a candidate future source in `game-modes.md`; not v1.

**Why it matters:** First demonstration of the full retuning loop
working as designed — a new mechanic entered, the sim caught its
second-order effect (subsidy → handle → Pearls, not the first-order
faucet inflation everyone would have guessed), and the anchor system
said exactly which knob to turn. The economy holds with the lottery in.

**Refs:** `docs/02-economy/simulations/bankroll.py`, `crate-game.py`,
`docs/02-economy/simulations/README.md`,
`docs/02-economy/currency-model.md`, `docs/00-project/roadmap.md`

## 11:13 — decision — Compliance rewritten around one wall: no real money, ever, in v1

**What happened:** `06-risks/compliance.md` rewritten for the settled
genre (it had predated the gambling-simulator decision). The organising
idea: a single load-bearing wall — **no real-money path in any
direction**. Shells earned only, Pearls wager-earned only, nothing
purchasable, nothing cashes out. That wall is what keeps a gambling
simulator outside gambling regulation (no consideration, no prize of
value) and outside the *Big Fish Casino* line of social-casino cases,
which all turned on *purchased* chips. Resolves roadmap question 5 for
v1: Shells are never purchasable; any post-v1 change requires a decision
entry and legal advice, in that order.

The rest of the posture: self-imposed 18+ age attestation at signup from
day one (matching the 17+/18+ store ratings the genre gets anyway);
disclose-everything on odds even though unpaid crates don't legally
require it; trading named as "the leak in the wall" (off-platform RMT is
how skins-gambling scandals start — late shipping, burn, tier
restrictions, no cash-out, and a compliance lean added to roadmap Q7
that Mythics should be untradeable); and an honest inventory of our own
risky patterns — the loss-rebate Pearls, the +EV lottery, referral
incentives, habit mechanics — each named with why it is safe behind the
wall. Old doc's operational risks carried forward, plus lottery pot
integrity as a new headline risk.

**Why it matters:** The last spec that predated the genre decision is
gone; every doc in the tree now describes the same game. Compliance is
no longer a "needs revisiting" flag but an agreed posture with one
tripwire: the wall comes down only with legal review. Remaining pre-code
work: the data model, rosters-to-JSON, the racer content pass, and
draft reviews.

**Refs:** `docs/06-risks/compliance.md`, `docs/00-project/roadmap.md`,
`docs/04-technical/hosting.md`, `docs/README.md`

## 11:27 — decision — Data model drafted: instances, content-as-code, logged CSPRNG

**What happened:** The last unwritten spec. Three forks put to Noah, all
recommendations accepted (`decisions/0015`): **every owned item is a
unique instance** (never deleted — salvage/fusion set `consumed`;
unlocks Mythic provenance, marketplace escrow, wear later), **the
catalogue is versioned JSON in the repo** (shipping a family is a
commit; drop-table versions stamped on every logged roll), and **logged
CSPRNG for v1** with provably-fair commit-reveal flagged as a post-v1
trust upgrade.

`04-technical/data-model.md` drafted from there — the rest was
derivation from standing specs: append-only dual-currency ledger with
cached-but-derivable balances and a drift-alarm reconciliation job;
one-transaction-per-economic-event; rounds/races/lottery tables with
per-player ticket caps enforced on insert; hidden racer weights
server-only; pity counters stored but reconcilable against the
crate-open audit log; hot table state in Postgres for v1 (Lambdas have
no memory; cache tier is a scale-time addition).

**Why it matters:** Every planned spec in the tree now exists — nothing
is "Not written" for the first time in the project's life. What remains
is content chores (rosters → JSON, naming the racers) and draft reviews.
Code can now be written against a schema instead of guessing one.

**Refs:** `docs/04-technical/data-model.md`,
`docs/decisions/0015-data-model-choices.md`, `docs/README.md`

## 11:37 — milestone — Content shipped as data 1.0.0; the racers have names

**What happened:** Both content chores done in one pass. The locked
rosters moved to their shipping format —
`03-cosmetics/content/data/{gear,skins,sets,drop-tables,racers}.json`,
content version 1.0.0, per doc-conventions rule 6 and `decisions/0015`.
The gear JSON carries the hover-skirt hide-flag structurally; the drop
tables carry rates, pity scopes (account-wide basic per 0011), salvage
values and the 1.5× direct multiplier, with `_note` fields pointing at
the docs of record until doc-generation exists.

And the sea races have a cast: **fourteen racers authored**
(`01-game/racers.md` + `racers.json`) — Sure Thing the metronomic
turtle favourite, Riptide the temperamental marlin speed star, Photo
Finish, Madame Ink, Second Wind, Undertow, Commodore, Fine Print the
methodical anglerfish, Knots, Moonjelly, Bubbles, Barnacle Bill, Long
Shot the hermit crab ("the three times he did are why the stands
exist"), and Sea Biscuit — a racing sea cucumber, the mascot bet. The
spread is deliberate: anchors make favourites bettable, chaos agents
make long odds dramatic, and the cyclical racers (Second Wind, Riptide)
are what makes form-watching pay off over weeks. Naming honoured the
Mythic rule — planned "The Commodore" and "The Auditor" renamed
(Commodore, Fine Print) because the definite article is reserved for
Mythics.

**Why it matters:** Content is not just decided but *shipped* — the
game can load its entire catalogue and cast from versioned files on day
one of API work. Nothing on the project's original blocking list
remains: what's left is Noah's draft reviews and the build itself.

**Refs:** `docs/03-cosmetics/content/data/`, `docs/01-game/racers.md`,
`docs/03-cosmetics/content/naming-conventions.md`, `docs/README.md`

## 11:56 — decision — Last roadmap questions closed; Mythics tradeable against advice

**What happened:** The three live roadmap questions answered
(`decisions/0016`):

- **Mythics are tradeable** on the marketplace — **taken against the
  compliance lean and against the recommendation offered**. Noah judged
  the drama worth it. Rather than a silent override, mitigations were
  written in: site-wide sale announcements, permanent public provenance
  including every price paid, a re-listing cooling-off window, ToS
  prohibition on account trading, and a **platform-wide Mythic-listing
  suspension switch that must exist in code from launch**. Mythic
  transfers become the top RMT monitoring signal. `compliance.md` now
  records the disagreement explicitly.
- **Visitor tips pay capped Shells** — third faucet.
- **Weekly rotation:** one family purchasable at the 1.5× direct price
  plus one spotlight set at 20% off (Set Crate 80 that week).

**Why it matters:** No open questions remain except the deliberately
parked post-v1 seasons one. The recorded disagreement on Mythics is the
point of the decisions tree working: the choice is made, the dissent is
preserved, and the reversal lever is named.

**Refs:** `docs/decisions/0016-mythic-trading-tips-and-rotation.md`,
`docs/06-risks/compliance.md`, `docs/03-cosmetics/rarity-tiers.md`,
`docs/00-project/roadmap.md`

## 12:00 — finding — Tips broke the anchor too; and the sim was under-pricing crates

**What happened:** Simulating the tip faucet before locking its cap —
the discipline the lottery established — caught two things.

First, tips at the initially-drafted 25 Shells/200-cap pushed committed
players to ~76 basic crates/week against the ~50 anchor, and worse than
the lottery case: **tips correlate with closet depth, so a large tip
faucet widens the committed/casual gap rather than lifting both.** Tips
were resized to a garnish — **10 Shells, 50/day cap** — which is the
honest shape for a social nicety anyway.

Second, raising trial counts for a stable read (earlier runs varied
62→78 crates/week between runs on 20 samples) exposed a **bug in
`bankroll.py`: it priced every crate at the gear price, 70, when the
roster is a gear/skin mix at 70/90.** Corrected to the blended 80. This
means yesterday's "prices must rise" conclusion after the lottery was
partly an artifact of the same bug — the 70/90 prices were right all
along.

Final validated state, all anchors held: committed **53** basic
crates/week, casual **19**; destruction **0.92 / 0.91**; zero busts at
typical stake sizing; first Legendary median 1.3 / 3.7 weeks, pity
firing 13.7%; set chase median 3 / 8 weeks.

**Why it matters:** Two lessons recorded in `currency-model.md` as
standing tensions: **three faucets now feed one sink with only ~0.02 of
headroom above the 0.90 floor**, so every future faucet must be
simulated before it ships; and simulation results need enough trials to
be stable before they are believed — a noisy read caused a real (if
harmless) mis-tune yesterday.

**Refs:** `docs/02-economy/simulations/bankroll.py`, `crate-game.py`,
`docs/02-economy/simulations/README.md`,
`docs/02-economy/currency-model.md`, `docs/02-economy/guardrails.md`

## 12:59 — finding — Production docs re-derived; the racers were never budgeted

**What happened:** The last stale corner of the tree — `05-production/`,
both files drafts written before the family restructure. Re-deriving
them surfaced three things nobody had noticed:

1. **The 14 racers are unbudgeted art, and they are a third of the
   work.** ~28 sprite sets (idle + racing) plus race furniture, ~55–85
   hours against the garments' 130–260. Every previous budget counted
   cosmetics only. Total launch art is now **~220–380 hours**, not
   130–260.
2. **The no-skin base is nearly free, because of families.** The rule
   reads as "66 garments need a base treatment," but in a
   grayscale+mask pipeline a base is a palette, and a family shares
   one — **11 records, not 66.**
3. **Gear families silently killed two of three slot-attention
   mitigations.** `slots.md` advised shipping fewer and cheaper items in
   the quiet slots (pants, shoes); under families every family has
   exactly one garment per slot, so counts are necessarily equal, and
   crates sell by axis not slot, so the pricing lever is gone too. Only
   exclusive FX survives. `slots.md` rewritten to say so, with the
   replacement lever: spend *design attention* unequally even though
   counts are equal.

Also fixed while verifying: `core-model.md` restated the hand-authored
asset count (~157, now stale) — a doc-conventions rule 2 violation, now
a link to the budget; `rarity-tiers.md` and `set-list.md` still
described the roster in pre-family terms; `naming-conventions.md`
promoted draft → agreed, having twice been used in anger.

The budget's cut-lever also changed shape and improved: cut *families*,
not items-per-slot — one Common family is six garments and twelve asset
files removed cleanly, leaving no hole in any slot.

**Why it matters:** Art can start with a correct estimate instead of one
that undercounts by a third. Every spec in the tree is now either agreed
or awaiting Noah's review; nothing is stale.

**Refs:** `docs/05-production/asset-budget.md`,
`docs/05-production/art-pipeline.md`, `docs/03-cosmetics/slots.md`,
`docs/03-cosmetics/core-model.md`

## 11:32 — finding — GitHub's ID-embedded OIDC sub claims broke the standard trust recipe

**What happened:** The first pipeline deploy failed at the credentials step:
`Not authorized to perform sts:AssumeRoleWithWebIdentity`, 12 retries. The
role's trust condition required the classic subject
`repo:noahndambukig/oddssea:ref:refs/heads/main`; CloudTrail's record of the
rejected call showed the token actually carried
`repo:noahndambukig@210443285/oddssea@1317725621:ref:refs/heads/main` —
GitHub now embeds the immutable user and repo IDs in the `sub` claim, and
`StringEquals` correctly refused the mismatch. The name-only recipe that
virtually every OIDC tutorial teaches no longer matches this repo's tokens.
The reason for GitHub's change is sound: names are re-registrable, so
name-based trust is open to resurrection attacks (delete or rename, attacker
re-registers the name, inherits the trust); numeric IDs cannot be reused.
Fix: the trust condition now accepts both forms via an IAM condition-value
array (OR semantics), with ownerId/repoId pinned in `infra/lib/config.ts`.
Diagnosis path worth remembering: CloudTrail → Event history →
AssumeRoleWithWebIdentity shows the exact rejected `sub` — no guessing.

**Why it matters:** Anyone reproducing this repo's CI/CD from the walkthrough
would have hit the same wall; the failure-mode table now covers it. The
ID-pinned form is also strictly better security than what was originally
designed — the incident upgraded the trust policy. Increment A's
pipeline-proof step is still pending: the fixed Cicd stack must be
redeployed (manual by design) and the workflow re-run.

**Refs:** `infra/lib/cicd-stack.ts`, `infra/lib/config.ts`,
`infra/README.md` failure-modes table, commit `141ee99` (pre-fix)

## 12:38 — milestone — Increment A complete: dev.oddssea.xyz live behind a keyless pipeline

**What happened:** The deployment skeleton's first increment is done end to
end. https://dev.oddssea.xyz serves over HTTPS (ACM cert, DNS-validated in
the delegated Route53 zone) from a fully private S3 bucket behind CloudFront
OAC, and the GitHub Actions pipeline deploys it with no stored credentials —
proof: the deploy role's `RoleLastUsed` stamped 2026-08-04T19:34Z after the
trust-policy fix, and the re-run went green. Exit criterion met: a push to
main reaches production with no human touching AWS.

Getting there surfaced four real failures, each now in the walkthrough's
failure-mode table: GoDaddy rejecting Route53's dotted FQDNs; NXDOMAIN
negative-caching after visiting the name pre-record (UBC resolver, SOA 900s);
CI unable to read the context cache until the account was pinned
(«StackAccountRegionNotSpecified»); and GitHub's ID-embedded OIDC sub claims
rejected by the name-only trust condition (see 11:32 finding). Also one
process near-miss: an unpushed fix re-run against the old commit — GitHub's
"Re-run jobs" always re-runs the same SHA.

Account hygiene landed en route: root user MFA'd and retired, IAM Identity
Center with an AdministratorAccess user, SSO CLI profile.

**Why it matters:** The project now has its delivery rail: every later
increment (login, API, ledger, games) ships through this pipeline. The
walkthrough (`infra/README.md`) captures the whole build well enough to
rebuild it from scratch, which was the milestone's second deliverable —
learning, not just infrastructure.

**Refs:** `infra/README.md`, `CLAUDE.md` current-state table, 11:32 entry
above, 2026-08-03 milestone entry, commit `4f74af2`

## 15:12 — decision — The app serves from the apex; dev.oddssea.xyz retired

**What happened:** Noah asked why the site still pointed at dev.oddssea.xyz
when the Increment B planning had settled "the apex serves the dev site."
The gap was real: the plan had engineered that choice as a 302 redirect
(apex → dev) to keep OAuth callbacks on one origin, preserving dev as the
canonical URL — a defensible reading, but not the one Noah meant. Resolved
explicitly: **oddssea.xyz is the canonical origin**; the app serves from
the apex itself, and dev.oddssea.xyz is retired outright — record and
certificate name removed, resolves to nothing. The generated
*.cloudfront.net hostname 302-redirects to the apex.

Timing made this nearly free: Increment B had not yet deployed, so the same
deploy that creates Cognito registers https://oddssea.xyz/callback from the
start — no client-config migration, no callback churn, nothing revalidated
twice. Caught after deploy, the flip would have touched the app client, the
config.json, and both certificates. Verified in the synthesized template:
distribution aliases ["oddssea.xyz"], apex callbacks, zero dev references.

**Why it matters:** This supersedes the 2026-08-03 "dev.oddssea.xyz for
now" choice and simplifies the round-12/13 review dispositions about the
apex-to-prod cutover — the apex is no longer parked-then-cut-over; it IS
the product URL, and a future prod story becomes a promotion question
rather than a DNS migration. Also a process note worth keeping: an option
label ("apex serves the dev site") that two people read differently is a
requirements bug — the redirect-vs-canonical distinction should have been
its own question.

**Refs:** `infra/lib/config.ts`, `infra/lib/constructs/web.ts`,
`infra/README.md` Part 6, 2026-08-03 journal (URL choice), commit `fd2686f`
(pre-flip)

## 18:55 — milestone — Increment B complete: real accounts, hand-written PKCE, 18+ gate live

**What happened:** Login works end to end at https://oddssea.xyz. Positive
evidence rather than absence of error: the user pool holds one CONFIRMED
user with `email_verified: true` and
`custom:age_attested_at: 2026-08-05T01:30:17.292Z` — meaning the full chain
ran for real (hosted signup → emailed verification code → the compliance
gate → an UpdateUserAttributes write with an access token carrying
`aws.cognito.signin.user.admin`). config.json carries the deploy-time
Cognito values; auth.oddssea.xyz answers with a valid certificate.

Two deploy failures on the way, both instructive and both now in the
walkthrough's failure tables. First: `Stack is in UPDATE_IN_PROGRESS and can
not be updated` — CloudFormation is single-writer, and a deploy started
before the apex flip was still running the slow (15–60 min) Cognito domain
create. Second, and the better lesson: `Tried to create resource record set
[oddssea.xyz., A] but it already exists`. The apex flip had renamed the
record construct, so CloudFormation read it as a *replacement* —
create-before-delete — which collided with the existing ApexRecord that
still owned the same physical name. Fix: keep the construct id of the
resource whose physical form survives (verified against
describe-stack-resources), letting the obsolete dev record be a plain
deletion. Logical IDs are identity; renames are replacements.

**Why it matters:** The skeleton now has authenticated users, which unblocks
everything with a `sub` attached to it. The 18+ attestation is the first
compliance requirement implemented in code rather than specced
(`docs/06-risks/compliance.md` "from day one"), and its interim home is a
Cognito custom attribute that migrates to the players table with the ledger.
`docs/decisions/0017` gates that same milestone on replacing sessionStorage
with a backend-for-frontend. Increment C — one Lambda behind API Gateway
with a JWT authorizer — is now the remaining piece of the skeleton, and
`IssuerUrl` output already points at the JWKS it will verify against.

**Refs:** `infra/lib/constructs/auth.ts`, `web/src/auth/`,
`docs/decisions/0017-tokens-in-sessionstorage-until-money-exists.md`,
`infra/README.md` Part 6, commits `fd2686f` `9e05ed3` `1b7b056`

## 21:15 — finding — A 200 is not proof a file exists: the dev-server SPA fallback broke local config loading

**What happened:** Driving Increment B's verification checks in a real
browser surfaced a bug static verification could not: `npm run dev` failed
at boot with `Unexpected token '<', "<!doctype "... is not valid JSON`.

`runtime-config.ts` fetches `/config.json` and, if the response is `ok`,
parses it — falling back to `web/.env.local` otherwise. But Vite's dev
server (like the deployed CloudFront error mapping, deliberately) answers
*any* unmatched path with `index.html` and **HTTP 200**, so a file that does
not exist arrives as HTML wearing a success status. `response.ok` was true,
`.json()` choked on `<!doctype`, and the documented `.env.local` fallback
was unreachable — it had never worked. Production was never affected: there
the file genuinely exists and is served as `application/json` (verified by
comparing `curl -I` against both origins — dev returned `text/html`).

Fixed by checking the content type before parsing and guarding the parse, so
the branch falls through cleanly instead of crashing the app on startup.

**Why it matters:** Fourteen rounds of adversarial plan review, a clean
typecheck, a clean build and a passing credential-free synth all missed
this, because none of them ran the program. The bug lived in the gap between
"the code is correct" and "the environment behaves as assumed" — and the
assumption at fault (404 means absent) is one almost every SPA dev server
and CDN quietly violates. Worth generalising: **an HTTP status describes
what the server chose to say, not what exists.** Content-type checks belong
on any fetch whose absence is a supported case.

**Refs:** `web/src/runtime-config.ts`, `infra/README.md` Increment B failure
table

## 22:30 — decision — Increment C planned; the API gets its own name

**What happened:** The Increment C planning session ran, per the standing
agreement that a milestone plan does not pre-authorize its phases. Most of
C was already fixed — HTTP API v2, `/health` public, `/me` behind a JWT
authorizer with `authorizationScopes: ['openid']`, CORS spelled out — so
the session reduced to one genuinely open decision: where the API lives.
Noah chose **`api.oddssea.xyz`** over the raw `execute-api` URL, against
the recommendation on the table, for name-family consistency with
`auth.oddssea.xyz` and a URL that survives API recreation.

**Why it matters:** Two consequences were priced in explicitly. First, the
prod cutover debt is now **three names deep** — the apex, `auth`, and now
`api` are all owned by the dev stack, and the eventual prod environment
inherits all of them in one planned DNS window; recorded here so that plan
cannot claim surprise. Second, the pretty name resurrects Increment B's
ordering race (c): a custom-domain URL is a computed constant rather than a
CloudFormation reference, so nothing naturally orders the site upload after
the API exists — the raw URL would have solved this for free through the
dependency graph. Same cure as B: an explicit `ready` dependency group, the
site publishes last. A decision about a *name* turned out to be a decision
about *deployment ordering*, which is worth remembering.

**Refs:** the apex decision (15:12 above); build lands in `infra/README.md`
Part 7 when built
